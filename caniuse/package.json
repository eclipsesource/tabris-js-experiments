{
  "name": "es6tests",
  "description": "Tests for the existance of JavaScript features/APIs.",
  "dependencies": {
    "http-server": "^0.9.0",
    "tabris": "^2.0.0"
  },
  "scripts": {
    "babel": "babel src -d lib && node ./lib/tests.js",
    "tabris": "babel src -d lib && hs"
  },
  "main": "app.js"
}
